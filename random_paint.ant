set desc = "randomly wanders in straight lines, generating interesting patterns!";
set speed = 32;
set fps = 16;
set loop = 1;
set sleep = 50;
set border = die;
set colors = bin;

ant main {
	(D2, D1) = buf(R2, R1);
	C0 = 1;
}

set desc = "Langton's Ant";
set size = 64;
set border = die;
set colors = bin;

fn main = in_color => out_color {
	left = in_color;
	right = -in_color;

	CC = 1;
	out_color = -in_color;

	D1 = or(left, right);
	D2 = left;
}

ant main {
	C2 = main(C2);
}

set size = 4;
set start = top_left;

ant main {
	C0 = -and(-T2, -T1, -T0);

	turn_right = and(-T2, -T1, +T0);
	turn_left =  and(+T2, -T1, -T0);

	D1 = or(turn_right, turn_left);
	D2 = turn_left;

	AX = and(+T2, -T1, +T0);
}

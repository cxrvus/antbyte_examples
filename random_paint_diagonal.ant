set desc = "randomly wanders in diagonal lines, generating interesting patterns!";
set tpf = 32;
set fps = 16;
set loop = 1;


set colors = bin;
set border = die;

ant main {
	D0 = -M0;
	(D2, D1) = buf(R2, R1);
	C0 = 1;
	M0 = 1;
}

use "../langton";

set desc = "Langton's Ant, but self-replicating";

set size = 128;
set border = die;
set colors = bin;


ant main {
	() = langton();

	A1 = -or(T2, T1, T0);
}

ant clone = 2 {
	() = langton();
	AX = CX;
}

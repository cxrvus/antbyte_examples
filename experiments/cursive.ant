use "../langton2";

set desc = "Langton's Ant, but self-replicating (+other variation)";

set size = 128;
set colors = bin;

ant main {
	() = langton2();

	A1 = -or(T2, T1, T0);
}

ant clone = 2 {
	() = langton();
	AX = CX;
}
